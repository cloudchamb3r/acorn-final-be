<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.finals.mapper.friendMapper">
    <select id="findAll" resultType="friendEntity">
        select * from friend
    </select>

    <select id="findOneById" parameterType="int" resultType="friendEntity">
        select * from friend
        where id = #{id}
    </select>

    <insert id="insert" parameterType="int">
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select friend_seq.nextval from dual
        </selectKey>
        insert into friend (id, nickname)
        values (#{id}, #{nickname})
    </insert>

    <update id="updateById" parameterType="friendEntity">
        update friend
        set nickname = #{nickname}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        delete from friend
        where id = #{id}
    </delete>
</mapper>